// export const allowOnlyNumber = (value) => {
//   return value.replace(/[^0-9]/g, '')
// }

// export const allowOnlyFloatNumber = (value) => {
//   return value.replace(/[^0-9.]/g, '')
// }

// export const castNumberToFixed = (
//   value: number | string,
//   digits = 2,
//   defaultValue = null as null | number
// ) => {
//   return parseFloat(Number(value).toFixed(digits)) || defaultValue
// }

// export const getNumberValue = (num: string | undefined | null | number) =>
//   num ? (typeof num === 'number' ? num : parseFloat(num)) : 0
